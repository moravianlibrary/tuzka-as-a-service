[project]
name = "taas-client"
version = "0.1.0"
requires-python = ">=3.11"
description = "Python client for taas OCR service"
dependencies = [
  "httpx>=0.27",
  "websockets>=12.0",
  "zstandard>=0.22",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio"]
